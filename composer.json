{
	"name": "madbob/mail_fulltextsearch",
	"description": "Extension to the Full Text Search app to index your users' mail.",
	"license": "AGPL-3.0-or-later",
	"authors": [
		{
			"name": "Roberto Guido",
			"email": "info@madbob.org",
			"homepage": "https://www.madbob.org/"
		}
	],
	"autoload": {
		"psr-4": {
			"OCA\\Mail_FullTextSearch\\": "lib/"
		}
	},
	"scripts": {
		"post-install-cmd": [
			"@composer bin all install --ansi"
		],
		"post-update-cmd": [
			"@composer bin all install --ansi"
		],
		"lint": "find . -name \\*.php -not -path './vendor/*' -not -path './vendor-bin/*' -not -path './build/*' -print0 | xargs -0 -n1 php -l",
		"cs:check": "php-cs-fixer fix --dry-run --diff",
		"cs:fix": "php-cs-fixer fix",
		"psalm": "psalm --threads=1 --no-cache",
		"test:unit": "phpunit tests -c tests/phpunit.xml --colors=always --fail-on-warning --fail-on-risky",
		"openapi": "generate-spec",
		"rector": "rector && composer cs:fix"
	},
	"require": {
		"php": "^8.4",
		"bamarni/composer-bin-plugin": "^1.8",
		"php-mime-mail-parser/php-mime-mail-parser": "^9.0"
	},
	"require-dev": {
		"nextcloud/ocp": "dev-stable34",
		"roave/security-advisories": "dev-latest"
	},
	"config": {
		"allow-plugins": {
			"bamarni/composer-bin-plugin": true
		},
		"optimize-autoloader": true,
		"sort-packages": true,
		"autoloader-suffix": "Mail_FullTextSearch",
		"platform": {
			"php": "8.4"
		}
	}
}
